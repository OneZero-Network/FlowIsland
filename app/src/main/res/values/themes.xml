<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. Compose owns all real theming (core.ui.theme.Theme.kt);
         this XML theme exists only because the manifest and the splash screen need
         a resource-defined theme before the first Compose frame renders. -->
    <style name="Theme.FlowIsland" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
        <item name="android:windowBackground">@color/flowisland_background_light</item>
    </style>

    <style name="Theme.FlowIsland.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/flowisland_background_light</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.FlowIsland</item>
    </style>

</resources>
